readme file

create iam user and provided permission to elastic beanstalk admin access

create access key and secret key for that user using cli mode when asked in gui

goto github repo and select the repo called docker-course-compose-into-emoji-app and right side top you'll see the settings, secrets and variables -- actions--repository secrets add access key and secret key which we created for hat user
